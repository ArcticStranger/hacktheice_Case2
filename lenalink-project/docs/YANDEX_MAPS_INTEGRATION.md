# Интеграция Yandex Maps API в LenaLink

Данная документация описывает интеграцию Yandex Maps API для отображения маршрутов в системе мониторинга билетов LenaLink.

## Описание функциональности

Интеграция позволяет пользователям:

- Просматривать интерактивную карту маршрута билета
- Видеть точки отправления, прибытия и пересадки
- Навигацию по карте с масштабированием
- Получение дополнительной информации о локациях

## Архитектура решения

### Компоненты системы

1. **YandexMap.vue** - основной компонент карты
2. **TicketMonitoring.vue** - интеграция карты в интерфейс мониторинга
3. **Mock данные** - для тестирования в разработке

### Схема взаимодействия

```
TicketMonitoring.vue
    ↓ (props: route)
YandexMap.vue
    ↓ (Yandex Maps API)
    ↓ (geocode API)
Яндекс.Карты
```

## Настройка API

### 1. Получение API ключа

1. Перейдите на [console.developer.tech.yandex.ru](https://console.developer.tech.yandex.ru)
2. Создайте новый проект или выберите существующий
3. Добавьте API "JavaScript API и HTTP Geocoder API"
4. Скопируйте API ключ

### 2. Настройка в проекте

#### Вариант 1: Через переменные окружения

```bash
# .env файл
VITE_YANDEX_MAPS_API_KEY=ваш_api_ключ_здесь
```

#### Вариант 2: В компоненте

```javascript
// В YandexMap.vue
const apiKey = process.env.VITE_YANDEX_MAPS_API_KEY || ''
```

### 3. Обновление компонента

В файле `src/components/YandexMap.vue` обновите загрузку API:

```javascript
loadYandexMaps() {
  return new Promise((resolve, reject) => {
    if (window.ymaps) {
      resolve()
      return
    }

    const apiKey = process.env.VITE_YANDEX_MAPS_API_KEY || ''
    const script = document.createElement('script')
    script.src = `https://api-maps.yandex.ru/2.1/?apikey=${apiKey}&lang=ru_RU&load=package.full`
    // ...
  })
}
```

## Функциональность карты

### Отображаемые элементы

1. **Точки маршрута**:

   - 🟢 Зеленая - отправление
   - 🔴 Красная - прибытие
   - 🟡 Желтая - пересадка

2. **Линия маршрута**:

   - Синяя линия соединяет все точки маршрута
   - Плавные анимации при изменении

3. **Интерактивность**:
   - Масштабирование
   - Перемещение по карте
   - Всплывающие подсказки

### API события

```javascript
// Обработка изменения границ карты
@bounds-changed="onMapBoundsChanged"

// Объект события
{
  center: [lat, lon],
  zoom: number
}
```

## Структура данных

### Входные данные (route)

```javascript
{
  segments: [
    {
      from: 'Москва (VKO)',
      to: 'Якутск',
      transport_type: 'air',
      provider: 'S7 Airlines',
    },
  ]
}
```

### Геокодирование

Автоматическое преобразование названий городов в координаты:

- Используется Yandex Geocoder API
- Кеширование запросов
- Обработка ошибок

## Тестирование

### Локальная разработка

1. Без API ключа карта работает в демо режиме
2. Mock данные обеспечивают реалистичное отображение
3. Все функции UI работают корректно

### Тестовые маршруты

```javascript
// Mock данные включают:
- Москва → Якутск (авиарейс)
- Москва → Санкт-Петербург (разные варианты)
- Международные маршруты
```

### Проверка функций

1. **Загрузка карты**:

   ```javascript
   // В консоли браузера
   console.log(window.ymaps) // должно быть определено
   ```

2. **Геокодирование**:
   ```javascript
   // Проверка геокодирования городов
   ymaps.geocode('Москва').then((res) => {
     console.log('Москва:', res.geoObjects.get(0).geometry.getCoordinates())
   })
   ```

## Производительность

### Оптимизации

1. **Ленивая загрузка**: карта загружается только при необходимости
2. **Кеширование координат**: повторные запросы не делаются
3. **Debouncing**: обработка изменений маршрута с задержкой
4. **Cleanup**: корректное удаление карты при закрытии компонента

### Лимиты API

- **Запросы в сутки**: 1000 (бесплатный тариф)
- **Геокодирование**: встроено в JavaScript API
- **Кеширование**: рекомендуется на стороне клиента

## Безопасность

### Рекомендации

1. **API ключи**: используйте переменные окружения
2. **Рефереры**: ограничьте ключ по доменам
3. **Мониторинг**: отслеживайте использование API
4. **Логирование**: записывайте ошибки геокодирования

### Конфигурация ключа

```
Домены:
- localhost (для разработки)
- ваш-домен.com (продакшн)
- *.ваш-домен.com (поддомены)
```

## Troubleshooting

### Частые проблемы

1. **Карта не загружается**:

   - Проверьте API ключ
   - Убедитесь в правильности домена
   - Проверьте консоль браузера на ошибки

2. **Геокодирование не работает**:

   - Проверьте подключение к интернету
   - Убедитесь в правильности названий городов
   - Проверьте лимиты API

3. **Маршрут не отображается**:
   - Проверьте структуру данных route
   - Убедитесь в наличии segments
   - Проверьте консоль на ошибки

### Отладка

```javascript
// Включение логирования
console.log('Map initialization:', this.route)
console.log('Yandex Maps loaded:', !!window.ymaps)

// Проверка координат
this.route.segments.forEach((segment) => {
  console.log(`${segment.from} → ${segment.to}`)
})
```

## Будущие улучшения

### Планируемые функции

1. **Визуализация транспорта**:

   - Иконки разных типов транспорта
   - Анимация движения

2. **Расширенная информация**:

   - Пробки на маршруте
   - Расчетное время в пути
   - Альтернативные пути

3. **Интеграция с реальными данными**:
   - GPS трекинг
   - Задержки рейсов
   - Погодные условия

### Техническая модернизация

1. **WebGL рендеринг** для больших маршрутов
2. **Кластеризация точек** для массовых данных
3. **Offline режим** с кешем карт
4. **Экспорт в различные форматы** (PDF, изображения)

## Заключение

Интеграция Yandex Maps API значительно улучшает пользовательский опыт в системе мониторинга билетов LenaLink, предоставляя визуальное представление маршрутов и делая интерфейс более интуитивным и информативным.

Для получения поддержки или сообщения об ошибках обращайтесь к команде разработки.
